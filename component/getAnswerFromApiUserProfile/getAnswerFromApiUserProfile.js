import { api } from "../../api/userInfo"


export function  getAnswerFromApiUserProfile(setRec , param ) {
    
   
        
            // api(param).then((e) => {
            //     setRec(e)
            // })
            
        
    
    
}